CONSTANTS
  Nodes = {n1, n2}
  Keys = {k1, k2}
  Msgs = {m1, m2}
  MaxLogLen = 3

INIT Init
NEXT Next

INVARIANT TypeInvariant
INVARIANT CommittedOffsetValid
INVARIANT AppendOnly
INVARIANT CommittedMonotonic
INVARIANT PollViewValid
